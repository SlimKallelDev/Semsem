/**
 * Shared app-chrome rules: header under logo + Home toolbar dividers
 * should read as the same stroke weight on all devices (hairlineWidth varies).
 */
export const CHROME_DIVIDER_HEIGHT = 1;
export const CHROME_DIVIDER_COLOR = "#E5EDE8";
